{
  "name": "world",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm-run-all --parallel watch serve",
    "build": "webpack",
    "watch": "webpack --watch",
    "serve": "http-server -c-1 target/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kamichidu <c.kamunagi@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0",
    "npm-run-all": "^3.0.0",
    "webpack": "^1.13.1"
  }
}
